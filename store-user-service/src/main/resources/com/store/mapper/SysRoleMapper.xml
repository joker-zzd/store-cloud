<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.store.mapper.SysRoleMapper">

    <resultMap id="BaseResultMap" type="com.store.domain.Role">
            <id property="id" column="id" />
            <result property="name" column="name" />
            <result property="roleKey" column="role_key" />
            <result property="sort" column="sort" />
            <result property="status" column="status" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="Base_Column_List">
        id,name,role_key,sort,status,create_time,
        update_time
    </sql>

    <select id="selectRoleKeysByUserId" resultType="java.lang.String">
        select sr.role_key
        from sys_role sr
        inner join sys_user_role sur on sr.id = sur.role_id
        where sur.user_id = #{userId}
          and sr.status = 1
    </select>
</mapper>
